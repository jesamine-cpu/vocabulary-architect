# Vocabulary Architect Agent Rules

本文件適用於未來所有處理 Vocabulary Architect 專案的 Codex Agent，必須遵守。

## 1. 核心原則

- 採最小幅度修改（minimal changes）。
- 不得為了新增功能而大幅重寫 `index.html`。
- 不得任意改變既有 UI、版面、配色與操作流程。
- 不得自行捏造教材內容。
- 無法確定的教材內容必須標記「需教師確認」。

## 2. 既有功能保護

修改前後必須確認：

- 原有 49 個函式保留。
- T1–T7 任務功能保留。
- 教師編輯功能保留。
- PDF 匯入功能保留。
- 語音辨識與語音播放功能保留。
- 學生登入功能保留。
- localStorage 學習進度保留。
- 報告與 PDF 輸出功能保留。
- 不得因新增冊別功能造成 regression。

## 3. 教材資料

- `moe_vocab_data.json` 為正式 canonical vocabulary data source。
- `index.html` 不得另外維護第二份完整單字資料。
- B1、B3、B5 及未來 B2、B4、B6 必須依冊別與 Unit 正確載入。
- 不得自行修改課本原始英文、中文意思或詞性。
- 教材來源沒有提供、且無法可靠判定的內容，保留空值並標記「需教師確認」。

## 4. 音節規則

- `syllables` 欄位以實際英文發音為判斷基準。
- `/` 代表依實際發音劃分的音節邊界，不是平均依字母位置切割。
- 不可只使用拼字演算法自動判斷音節。
- 單字、片語、句子必須分開處理。
- 多字片語必須逐詞判斷。
- 縮寫、所有格、連字號、括號、複合詞必須另外檢查。
- 若不同權威字典存在合理差異，標記「需教師確認」，不得自行選定。
- 已確認範例：
  - `very` → `ver/y` → 2 syllables
  - `really` → `real/ly` → 2 syllables（依本教材採用發音）
  - `T-shirt` → `T/shirt` → 2 syllables
  - `drawer` → `drawer` → 1 syllable（依本教材採用美式發音）

## 5. QA 規則

每次教材資料更新後必須檢查：

- JSON.parse 是否成功。
- 總筆數是否正確。
- 冊別與 Unit 是否正確。
- 拼字。
- 中文意思。
- 詞性。
- 重複資料。
- 空白欄位。
- 標點與大小寫。
- 音節數。
- 音節切分位置。
- 片語與句子。
- JavaScript 語法。
- 原有網站功能是否保留。

## 6. GitHub 工作流程

正式 `main` 不直接修改。

流程：

建置 Agent
→ 測試分支
→ QA Agent 審核
→ 建置 Agent 修正
→ QA Agent 複驗
→ 教師確認
→ 才可 Merge 到 `main`

QA 未通過時不得建議 Merge。

## 7. 修改報告

每次修改完成必須回報：

- 修改了哪些檔案。
- 每個檔案修改目的。
- 新增／刪除行數。
- 教材資料修改筆數。
- 測試結果。
- 尚未解決或需教師確認的項目。
